/*
Desenvolvimento de sistema para um restaurante em Java.

Para este trabalho o banco de dados que vocês irão utilizar será o Mysql. -- OK

A principal finalidade da aplicação é gerenciar os clientes, estoque, fornecedor, perfil, produto, comanda e os ítens da comanda além dos usuários que utilizarão o sistema.

·         O sistema deverá possuir login e senha. -- OK

·         A tabela cliente deverá possuir os seguintes campos: (id,nome, cpf, cep, endereco, bairro, cidade, uf, sexo, estadocivil, receberPromocoes[boolean]);

·          A tabela comanda deverá possuir o seguinte campo: (id);

·         A tabela estoque deverá possuir o seguinte campo: (id, produto, qtdminima, qtdmaxima,qtdatual);

·         A tabela fornecedor deverá possuir os seguintes campos: (id,nome,cnpj, cep,endereco, bairro, cidade, uf);

·         A tabela item de comanda deverá possuir os seguintes campos: (id, Produto produto, qtde);

·         A tabela perfil deverá possuir os seguintes campos: (id, nome );

·         A tabela produto deverá possuir os seguintes campos: (id, descricao, unidade, preco, Fornecedor fornecedor);

·         A tabela usuario deverá possuir os seguintes campos: (id, nome, login, senha, ativo [boolean], Perfil perfil);

Senhas deverão estar armazenadas com algum tipo de criptografia no banco de dados.
Vocês também deverão desenvolver a janela de login. -- OK
O sistema deverá estar estruturado na organização que estudada em sala.
 */
package sistema;

/**
 *
 * @author Brunno Rodrigues
 */
public class Fazer {
    
}
